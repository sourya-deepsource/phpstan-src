<?php

namespace SomeOtherUseNamespace;

use Uses\Bar;
use Uses\Nonexistent; // could be namespace
use Uses\bar;
use Uses\OTHER_CONSTANT;
use Uses\LOREM;
use DATETIME;
use function Uses\foo as fooFunction;
use function Uses\Foo;
use const Uses\MY_CONSTANT;
